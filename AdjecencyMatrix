
//works strictly with the ReadGraph.java file, uses its specfic inputs etc.
// improvement should be using the vertices as the dimensions of the arrays



int[][] matrix = new int[vertices][vertices];
				for(int i = 0;i<e.length;i++){
					matrix[e[i].u][e[i].v] = 1;
					matrix[e[i].v][e[i].u] = 1;
				}

int upperBound(int[][] matrix) {
	int max = 0;
	int upperBound = 0;
	
	for(int i = 0; i < matrix[0].length; i++) {
		for(int j = 0; j < matrix.length; j++) {
			if(Matrix[i][j]) {
				upperBound++;
			}
		}
		if(max < upperBound)
			max = upperBound;
			upperBound = 0;
	}
	return max;
}
